import { Home, User, Sparkles, FolderGit2, Mail, type LucideIcon } from 'lucide-react'

// Maps the string icon keys from data.ts to their Lucide components.
export const navIcons: Record<string, LucideIcon> = {
  Home,
  User,
  Sparkles,
  FolderGit2,
  Mail,
}
